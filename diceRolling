#D6 Dice App

from random import randint

reroll = 'y'

while reroll == 'y':
    d1 = randint(1,7)
    d2 = randint(1,7)
    rollMultiplier = int(input("How many dice to roll?: "))
    result = ((d1 + d2) * rollMultiplier)
    print(result)
    reroll = input("Do you want to roll again? y/n:")
